# trust-monitor

## Dependencies:
- ```libpq-dev```
- ```librdkafka-dev```

## docker-compose deployment
- Move into the ```docker-compose``` directory
- ```$ docker-compose up -d --build``` (remove ```--build``` if it is not necessary to rebuild the docker images)